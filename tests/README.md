# CoScope Tests

- `tmp/` — scratch outputs from smoke tests (gitignored, safe to delete).
  Created automatically by `python -m coscope.main --test ...`.
- Unit tests will go at this top level as `test_*.py` (pytest).
