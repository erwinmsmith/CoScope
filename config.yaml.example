retrieval:
  public_similarity_threshold: 0.90
  minimum_pairwise_similarity: 0.85
  max_group_size: 16
  shared_candidate_k: 50
  rerank_k: 20

context:
  default_token_budget: 2000
  allow_provisional_owner_content: true

runtime:
  embedding_adapter: deterministic
  trace_raw_content: false
