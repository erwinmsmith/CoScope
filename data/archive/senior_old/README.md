# Senior Historical Archive

This directory is reserved for historical data/results from
`CoScope(1)/CoScope`.

Stage 7 found no local files under `CoScope(1)/CoScope/data`, so nothing was
copied here during this pass. If senior GoT/ToT results become available later,
archive old non-reproducible outputs here rather than mixing them into
`data/processed/`, `data/eval/`, or `data/exports/`.

