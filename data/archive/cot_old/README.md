# CoT Historical Archive

This directory contains historical CoT outputs imported from the old
`CoScope/coscope` project.

These files are kept as provenance only. Current reproducible inputs live under
`data/processed/cot/...`, and current evaluation/export outputs should be
written to `data/eval/` and `data/exports/`.

